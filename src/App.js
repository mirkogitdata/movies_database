import React from 'react';
import Routes from './routes';
import { HashRouter } from 'react-router-dom';

const App = () => (
   <HashRouter>
      <Routes />
   </HashRouter>
);

export default App;
